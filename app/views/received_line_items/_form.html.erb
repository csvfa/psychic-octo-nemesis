<%= form_for(@received_line_item) do |f| %>
  <% if @received_line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@received_line_item.errors.count, "error") %> prohibited this received_line_item from being saved:</h2>

      <ul>
      <% @received_line_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form role="form"> <!-- Bootstrap CSS styling -->
  <div class="form-group">
    <%= f.label :received_on %><br />
    <%= f.date_select :received_on %>
  </div>
  <div class="form-group">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="form-group">
    <%= f.label :payment_method %><br />
    <%= f.text_field :payment_method %>
  </div>

 <%= f.hidden_field :invoice_id, :value => @invoice.id %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
