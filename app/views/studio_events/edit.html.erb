<div class="container">
	<div class = "row">
		<div class = "col-md-11">
			<h1>Editing studio note <small>for <%= @booking %></small></h1>
		</div>
		<div class = "col-md-1">
			<%= button_to "Delete note", @studio_event, :method=>:delete, :class=>"btn btn-danger",:confirm=>"Are you sure?" %>
		</div>
	</div>
	<%= render 'form' %>
	<%= link_to 'Home', root_path %>
</div>